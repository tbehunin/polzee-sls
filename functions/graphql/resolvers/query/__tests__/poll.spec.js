// import poll from '../poll';
// import get from '../../../../../data/polls/get';

// jest.mock('get');

describe('pollResolver', () => {
  describe('when current user is the author', () => {
    describe('and has voted', () => {
      test('bar', async () => {
        // const pollData = {};
        // const voteData = {};
        // get.poll.mockResolvedValue(pollData);
        // get.vote.mockResolvedValue(voteData);

        // const args = { userId: '1', createTimestamp: 111 };
        // const context = { userId: '1' };
        // const expected = 'hello';
        // const actual = await poll(null, args, context);
        // expect(actual).toBe(expected);
      });
    });
    describe('and has not voted', () => {
    });
  });

  describe('when current user is NOT the author', () => {
    //
  });
});
